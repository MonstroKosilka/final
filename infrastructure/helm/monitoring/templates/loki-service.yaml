{{- if .Values.loki.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: {{ include "monitoring.ns" . }}
spec:
  selector:
    app: loki
  ports:
    - name: http
      port: 3100
      targetPort: 3100
      protocol: TCP
{{- end -}}