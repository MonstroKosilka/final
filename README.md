# Final — Full‑stack приложение с мониторингом

## 📌 Описание проекта

**Final** — это full‑stack веб‑приложение, состоящее из:

* **Backend** — Go API (HTTP, JSON, structured logs)
* **Frontend** — SPA на Vue.js, отдаётся через nginx
* **Monitoring stack** — Prometheus, Grafana, Loki, Promtail
<img width="1899" height="977" alt="main_page" src="https://github.com/user-attachments/assets/80d43db7-67d1-49f0-bb62-37728cb41d1b" />

---

## 🧱 Архитектура

```
┌──────────┐     HTTP      ┌────────────┐
│ Frontend │ ───────────▶ │  Backend   │
│  (Vue)   │               │   (Go)     │
└────┬─────┘               └────┬───────┘
     │ nginx                       │
     │                             │
     ▼                             ▼
┌──────────┐               ┌────────────┐
│ Grafana  │ ◀───────────▶ │ Prometheus │
│          │ ◀───────────▶ │    Loki    │
└──────────┘               └────────────┘
              ▲
              │
          Promtail
```

---

## 📁 Структура проекта

```
final/
├── .github/                      # CI/CD-workflow’ы и GitHub-конфигурации
├── backend/                     # Серверная часть (Go backend)
│   └── Dockerfile               # Сборка backend-контейнера
├── frontend/                    # Клиентская часть (Vue.js + NGINX)
│   ├── Dockerfile               # Сборка frontend-контейнера
│   └── default.conf             # Конфигурация сервера NGINX
├── monitoring/                  # Конфигурации для мониторинга
│   ├── prometheus/              # Prometheus — сбор метрик
│   │   └── prometheus.yml       # Конфигурация Prometheus
│   ├── loki/                    # Loki — агрегатор логов
│   │   └── config.yml           # Конфигурация Loki
│   └── promtail/                # Promtail — отправка логов в Loki
│       └── config.yml           # Конфигурация Promtail
├── minikube/                    # Настройки для локального Kubernetes-окружения (Minikube)
├── infrastructure/              # Инфраструктурный код (для развёртывания в Yandex Cloud)
├── screenshots/                 # Скриншоты и визуальные примеры приложения
├── .gitignore                   # Игнорируемые файлы Git
├── README.md                   # Главная документация проекта
├── docker-compose.yml          # Основной Compose-файл для локального запуска
└── docker-compose-latest.yml   # Альтернативный Compose-файл с обновлёнными образами
```

---

## 🚀 Локальный запуск (Docker compose)

### 1️⃣ Клонирование проекта

```bash
git clone https://github.com/MonstroKosilka/final.git
cd final
```

---

### 2️⃣ Сборка и запуск контейнеров

```bash
docker compose up -d --build
```

Docker автоматически:

* соберёт backend и frontend
* поднимет monitoring‑stack
* создаст volumes для хранения данных

#### Примечание

```bash
docker-compose-latest.yaml
```
Позволяет использовать последнии версии сервисов backend и frontend, опубликованные в репозитории GitHub

---

### 3️⃣ Доступные сервисы

| Сервис      | URL                                            | Описание       |
| ----------- | ---------------------------------------------- | -------------- |
| Frontend    | [http://localhost:8080](http://localhost:8080) | Web UI         |
| Backend API | [http://localhost:8081](http://localhost:8081) | REST API       |
| Prometheus  | [http://localhost:9090](http://localhost:9090) | Метрики        |
| Grafana     | [http://localhost:3000](http://localhost:3000) | UI мониторинга |
| Loki API    | [http://localhost:3100](http://localhost:3100) | Логи           |

Grafana по умолчанию:

```
login: admin
password: admin
```

---

## 📊 Метрики (Prometheus)

Backend экспортирует метрики по адресу:

```
GET /metrics
```

Prometheus автоматически собирает их каждые 5 секунд.

Проверка:

* Prometheus → **Status → Targets**
* backend должен быть в состоянии **UP**

---

## 🧾 Логи (Loki + Promtail)

Promtail собирает docker‑логи всех контейнеров и отправляет их в Loki.

Логи размечены label‑ом:

```
app = backend | frontend
```

### Просмотр логов

Grafana → **Explore → Loki**

Примеры запросов:

```logql
{app="backend"}
```

```logql
{app="frontend"}
```

```logql
{app="backend", level="error"}
```

---

## 🧹 Остановка проекта

```bash
docker compose down
```
